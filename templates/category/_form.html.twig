{{ form_start(form) }}
    <div class="formRow">
        {{ form_label(form.name, "Nom", {"label_attr": {"class": "formLabel"}}) }}
        {{ form_widget(form.name) }}
    </div>
    <button class="btn">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
